{
  "version": 2,
  "builds": [
    {
      "src": "server.js",
      "use": "@vercel/node",
      "config": { "includeFiles": ["student_data.json"] }
    }
  ],
  "routes": [
    { "src": "/(.*)", "destination": "server.js" }
  ]
}